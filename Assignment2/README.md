# Assignment structure
In each folder is the code for each assignent

Assignment 7 has 2 versions a standalone and a compiled version, the compiled version has a memory corruption error because of the linker probably using the same name for a class or something
## TODO 
Allow compatibility with my last assignment so i can browse through the executable code for each assignment 

### How to compile 
linux for assignment7 just do both cpp files the main file being first
```
g++ inputfile.cpp -o outfile
```
